<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mountain Store - Database Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto p-6">
        <div class="bg-white rounded-lg shadow-lg p-6">
            <h1 class="text-3xl font-bold mb-6 text-blue-600">🏔️ Mountain Store - Database Admin</h1>
            
            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
                <% Object.keys(stats).forEach(collection => { %>
                <div class="bg-blue-50 p-4 rounded-lg">
                    <h3 class="font-bold text-lg text-blue-800"><%= collection %></h3>
                    <p class="text-2xl font-bold text-blue-600"><%= stats[collection] %></p>
                    <p class="text-sm text-gray-600">documents</p>
                </div>
                <% }) %>
            </div>

            <h2 class="text-2xl font-bold mb-4">Collections</h2>
            <div class="grid gap-4">
                <% collections.forEach(collection => { %>
                <div class="bg-gray-50 p-4 rounded-lg border">
                    <div class="flex justify-between items-center">
                        <div>
                            <h3 class="font-bold text-lg">📊 <%= collection %></h3>
                            <p class="text-gray-600"><%= stats[collection] %> documents</p>
                        </div>
                        <a href="/collection/<%= collection %>" 
                           class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
                            View Data
                        </a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>